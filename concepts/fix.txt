 1. Fix Sudo (Switch to traditional version)
  This prevents Ansible from timing out while waiting for a password prompt.

   1 echo henryhenrik | sudo -S update-alternatives --set sudo /usr/bin/sudo.ws

 